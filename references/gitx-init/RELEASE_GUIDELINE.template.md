# Release Guideline — {{PROJECT_NAME}}

> Auto-generated by `gitx-init` ({{DATE}} · gitx-release {{GITX_VERSION}}).
> This file is the dev-session AI Agent's entry point for "how do I release this project?".
> Edit freely once generated — gitx-init refuses to overwrite without `--force`.

## 1. Project type

**Detected**: `{{PROJECT_TYPE}}`

Type signals (see `.gitx/policy.md` §1):
- `skill` → `skills/<name>/SKILL.md` present
- `mac` → `*.xcodeproj` / `Package.swift` / `src-tauri/Cargo.toml` present
- `both` → both signals present
- `empty` → neither (release contract deferred)

## 2. Quick start

```bash
# Skill release path (type=skill or both):
bash ~/.agents/skills/gitx-release/scripts/gitx-release.sh

# Mac release path (type=mac or both):
bash ~/.agents/skills/mac-release/scripts/mac-release.sh
```

Both paths require the corresponding skill to be installed at one of three CLI roots:
`~/.agents/skills/` (canonical) · `~/.claude/skills/` · `~/.config/opencode/skills/`.

## 3. Pre-flight checklist

- [ ] `bash tests/run_all.sh` → 0 fail
- [ ] `Release/CHANGELOG.md` top entry matches new version (`## vX.Y.Z — {{DATE}}`)
- [ ] `.sanitize-ignore` covers any project-specific whitelisted detector hits
- [ ] `VERSION` sidecar incremented (semver: patch / minor / major)
- [ ] No `*.sync-conflict-*` / `.syncthing.*.tmp` residue (Gotcha #34 / #35)
- [ ] Working tree clean OR uncommitted changes intentional

## 4. Release artifacts spec

| Artifact | Required? | Notes |
|---|---|---|
| `.skill` bundle | yes (type=skill) | Output of `skill-creator/package_skill.py` |
| `<project>-<ver>.tar.gz` source tarball | yes | Deterministic, honors `$SOURCE_DATE_EPOCH` |
| `<project>-<ver>-full.tar.gz` | yes | Includes `Release/<ver>/` tree |
| `checksums.txt` | yes | sha256 of every release artifact |
| `sbom.cyclonedx.json` | yes | CycloneDX 1.4 SBOM |
| `TOKEN_USAGE.json` | yes | Token-cost telemetry |
| `.app` / `.dmg` / `.pkg` | yes (type=mac) | Signed + notarized + stapled |

## 5. Audit gates

`release-audit.sh` runs ~14 chapters / 200+ checks. Key gates:
- §0_spec — SKILL.md ALLOWED_PROPERTIES whitelist (Anthropic skill-creator spec)
- §0b — install.sh helper usage + INSTALL.md schema (TKX 5-skill unified)
- §0c — gitx-init template integrity (this skill's own contract)
- §2 — required docs present + `TEST-SCENARIOS.md` soft-warn
- §4 — CHANGELOG truthfulness vs `VERSION`
- §7 — post-release sanity scan (red list, see §10)
- §11h — checksums.txt covers all artifacts

## 6. TKX policy reference

See `.gitx/policy.md` for the project-level excerpt of TKX Git Release policy v2.3.

## 7. Test-scenarios contract

`TEST-SCENARIOS.md` (project root) lists release-mode user scenarios. Soft-warned
since gitx-release v1.3.2 — missing the file produces a warning, not a hard fail.

## 8. CHANGELOG conventions

- Top-anchored: newest first.
- Header format: `## vX.Y.Z — YYYY-MM-DD` (literal hyphen, ASCII).
- TODO gate accepts the sentinel `<!-- gitx-auto-entry: ... -->` (replace before publish).

## 9. Versioning policy

- SemVer: major.minor.patch.
- Single source of truth: project-root `VERSION` sidecar. **Never** `SKILL.md metadata.version` (Gotcha #16).
- Bump rules: bugfix → patch; new feature / new gate / new behavior → minor; breaking → major.

## 10. Sanity-scan red list

`release-sanitize.sh` blocks 6 categories of leaks:

1. API keys / tokens / passwords (PCRE entropy + provider patterns)
2. Absolute user paths (`/Users/<name>/...`, `/home/<name>/...`)
3. Real email addresses
4. Public IP addresses
5. MAC addresses
6. SSH / GPG fingerprints

Use `.sanitize-ignore` (path-anchored, never bare basename) to whitelist intentional fixtures.

## 11. Multi-CLI install matrix

Every TKX skill installs into three CLI roots simultaneously:

| Root | Purpose |
|---|---|
| `~/.agents/skills/<name>/` | Canonical (Codex auto-discovery) |
| `~/.claude/skills/<name>/` | Claude Code symlink → canonical |
| `~/.config/opencode/skills/<name>/` | OpenCode symlink → canonical |

`install.sh` enforces byte-identical content across all three roots.

## 12. Handoff & gotchas

- Maintain `HANDOFF.md` (gitignored — user-level work memory, not git).
- Cross-machine portable backup at `<project>/.memory/`.
- Known traps logged inline in `HANDOFF.md` `## 🐛 Known Gotchas` (or `GOTCHAS.md` in v2 layout).
- 37 gotchas tracked in gitx-release as of {{DATE}} — see upstream for canonical list.
